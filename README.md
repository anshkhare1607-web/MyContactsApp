UC-01: User Registration
Actor: New User

Description: User creates an account with email, password, and profile information.

OOP Concepts: User class with encapsulation (private fields), validation logic, password hashing

Design Patterns: Factory Pattern for creating different user types (FreeUser, PremiumUser), Builder Pattern for User object construction

Java Concepts: Input validation, exception handling, regular expressions for email validation
