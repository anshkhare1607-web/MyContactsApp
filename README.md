UC-02: User Authentication
Actor: Registered User

Description: User logs in with credentials to access their contact list.

OOP Concepts: Authentication interface, concrete implementations (BasicAuth, OAuth), polymorphism

Design Patterns: Strategy Pattern for different authentication methods, Singleton for SessionManager

Java Concepts: Password hashing with MessageDigest, session management, Optional for handling login results
